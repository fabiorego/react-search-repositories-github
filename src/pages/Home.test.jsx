test('Check for GitHub.', () => {
  expect('GitHub').toMatch(/GitHub/);
});

test('Check for Search .', () => {
  expect('Search').toMatch(/Search/);
});

test('Check for Search .', () => {
  expect(20).toBeLessThan(30);
});
